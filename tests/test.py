# All of the elements of this list should have the same face:
var = ["one",
       "two",
       three, "four",
       "five"]

# The argument "bar" in this function definition should have the same
# color as the list elements above, not the same color as "def foo":
def foo(bar):
    baz = [buzz]

    if (foo
           bar baz):
        buzz
